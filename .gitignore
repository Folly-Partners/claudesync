# Debug logs and temporary files
debug/
*.log
*.tmp

# Session-specific data
shell-snapshots/
session-env/
file-history/

# Statistics and analytics (may contain personal data)
statsig/
stats-cache.json

# Downloads directory (may contain sensitive files)
downloads/

# Local environment variables or secrets
.env
*.key
*.pem
auth_token

# OAuth session tokens (machine-specific, would cause conflicts)
.credentials.json

# Large session files (sync issues)
history.jsonl
claude.json

# Sync tracking (machine-specific)
.last_sync_date
sync-launchd.log

# Plugin marketplaces (can be re-downloaded)
plugins/marketplaces/