# Debug logs and temporary files
debug/
*.log
*.tmp
cache/
tmp/

# Session-specific data
shell-snapshots/
session-env/
file-history/

# Statistics and analytics (may contain personal data)
statsig/
stats-cache.json

# Downloads directory (may contain sensitive files)
downloads/

# Local environment variables or secrets
.env
.env.local
.env.*
*.key
*.pem
auth_token

# OAuth session tokens (machine-specific, would cause conflicts)
.credentials.json

# Large session files (sync issues)
history.jsonl
claude.json

# Sync tracking (machine-specific)
.last_sync_date
sync-launchd.log

# Plugin data (can be re-downloaded, contains machine-specific paths)
plugins/marketplaces/
plugins/cache/
plugins/installed_plugins.json

# Machine-specific settings (contains absolute paths and local permissions)
settings.local.json

# Session-specific data (machine-local, too large to sync)
projects/
todos/
paste-cache/
plans/
security_warnings_state_*.json
image-cache/

# Server build artifacts (regenerated by setup.sh)
servers/super-things/node_modules/
servers/super-things/dist/
servers/unifi/venv/
