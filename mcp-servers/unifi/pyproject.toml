[project]
name = "unifi-mcp-server"
version = "0.1.0"
description = "MCP server for Ubiquiti Unifi network management"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "pyunifi>=2.21",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
